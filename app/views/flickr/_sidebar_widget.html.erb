<div class="flickr">
  <ul>
    <% photos.each do |row| %>
      <li>
        <% row.each do |p|    %>
          Hello
          <%= link_to(image_tag(FlickRaw.url(p)), url: FlickRaw.url(p)) %>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>